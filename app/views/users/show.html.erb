<div id="toppage">
<%=image_tag("loladominguez1.png", height: '250', width: '250', :class => "avatar gravatar2")%>


 <%
=begin%>
 <%= gravatar_for @user, :class => "avatar"%> 
<%
=end%>
</div>
<section class="search-box">
  <div class="container-fluid" id="usercontainer">
    <div class="row">
      <div class="col-md-12 listing-block">
      
          <div class="mediacal" >
            <div class="user-label">
              <h1>
             
                <strong><%= @user.firstname %> <%= @user.lastname %></strong>
              </h1>
              <%= link_to 'Settings', edit_user_path(@user) %>
            </div>
          </div>  
          <div class=" mx-0 px-0 calbox">
            <div class="contain-user">
              <div class="usersaved">
                <h3>Upcoming Classes</h3>
                  <ul>
                    <% @user.events.each do |event| %>
                      <li><%= event.name %> <%= event.event_start.strftime("%A, %d %b %Y %l:%M %p") %></li>
                    <% end %>
                  </ul>
              </div>
              <div class="usersaved">
                <h3>Favorite Gyms</h3>
                <ul>
                  <% @user.gyms.each do |gym| %>
                    <li><%= link_to gym.name, gym %></li>
                  <% end %>
                </ul>
              </div>
          </div>
        </div>

    </div>
  </div>
</section> 
